export enum EmailType {
  //remember to keep this file in sync with prisma schema
  EMAIL_CONTINUATION = 'EMAIL_CONTINUATION',
  RESET_PASSWORD = 'RESET_PASSWORD',
  CHANGE_EMAIL_ADDRESS = 'CHANGE_EMAIL_ADDRESS',
}
